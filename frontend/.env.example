# ----- Smart Regression System :: Frontend env -----

# Base URL of the FastAPI backend. Must include the API version prefix.
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# Client-side upload guard. Must match backend MAX_UPLOAD_MB.
NEXT_PUBLIC_MAX_UPLOAD_MB=25
